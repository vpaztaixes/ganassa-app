# App
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000

# Docker local Postgres service
DATABASE_URL=postgresql://ganassa:ganassa_dev_password@db:5432/ganassa
POSTGRES_DB=ganassa
POSTGRES_USER=ganassa
POSTGRES_PASSWORD=ganassa_dev_password

# Auth
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=24h

# Translation
GOOGLE_TRANSLATE_API_KEY=

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
