# App
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000

# Database (managed Postgres or local host)
DATABASE_URL=postgresql://user:password@localhost:5432/ganassa

# Auth
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=24h

# Translation
GOOGLE_TRANSLATE_API_KEY=

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
